# 外请人员打卡管理系统 - 项目总结

## 一、项目概述

### 1.1 项目背景
本系统旨在解决酒店/医疗机构外请人员的打卡管理问题，实现统一注册入口、岗位驱动费用统计、审核考核闭环管理。

### 1.2 核心目标
- **统一注册**：所有人通过扫码注册，不区分外请/内部
- **岗位驱动统计**：外请/内部区分只在岗位类型上，用于费用统计
- **审核考核闭环**：审核员审核打卡记录、分配岗位、月度考核

### 1.3 原型文件清单
| 文件 | 说明 |
|------|------|
| `temp-worker-miniapp-demo.html` | 小程序原型（审核员视角 + 董事长视角） |
| `temp-worker-demo.html` | 管理端原型（人员/岗位/审核员/考核/统计） |
| `scan-checkin-flow-demo.html` | 扫码注册流程演示 |

---

## 二、角色架构

### 2.1 角色矩阵

| 角色 | 入口 | 核心功能 | 是否需审核 |
|------|------|---------|-----------|
| **临时工/内部员工** | 扫码/小程序搜索 | 注册、打卡（我的岗位+临时岗位） | 打卡需审核 |
| **审核员** | 小程序 | 审核打卡、分配岗位、月度考核、补录打卡 | 补录默认通过 |
| **董事长** | 小程序 | 只读看板（打卡/审核概览）、只读统计（总览/部门分析） | 无审核权限 |
| **超级管理员** | PC管理端 | 人员管理、岗位管理、审核员管理、考核管理、统计分析 | - |

### 2.2 权限说明

#### 审核员
- 只能审核自己负责岗位的打卡记录
- 扫码人员的打卡记录所有审核员可见，但只能分配到自己管理的岗位
- 考核只显示自己负责岗位中需要考核的人员
- 可查看自己所辖岗位的月度统计

#### 董事长
- 只读权限，无打卡/审核/考核入口
- 可查看全公司每日打卡审核情况
- 可查看统计总览和部门分析

---

## 三、业务流程

### 3.1 注册流程（统一入口）

```
扫码（小程序码）
  ↓
微信授权登录（自动获取openid）
  ↓
填写姓名（必填）+ 获取手机号（微信一键获取）
  ↓
首次进入：只看到「临时岗位」→ 打卡
  ↓
审核员分配岗位
  ↓
下次进入：看到「我的岗位 + 临时岗位」
```

### 3.2 打卡流程

```
进入打卡页
  ↓
选择岗位（我的岗位 / 临时岗位）
  ↓
确认打卡
  ↓
普通打卡：进入待审核列表
补录打卡：直接通过审核（无需再次审核）
```

### 3.3 审核流程

```
审核员进入审核Tab
  ↓
查看待审核记录（仅自己负责的岗位）
  ↓
操作：通过 / 修正（调整岗位/金额） / 驳回 / 分配岗位（扫码人员）
  ↓
通过后进入已审核列表，计入统计
```

### 3.4 考核流程

```
月底审核员进入考核Tab
  ↓
查看需考核岗位的人员（仅自己负责的岗位）
  ↓
操作：考核通过（100%） / 扣分结算（80%/70%/50%/0%）
  ↓
二次确认弹窗（防误点）
  ↓
已完成考核后可修正（重新考核/撤销考核）
```

### 3.5 补录流程

```
审核员点击「补录打卡」按钮
  ↓
填写姓名（自动匹配已注册用户，填充手机号）
  ↓
选择岗位（仅自己负责的岗位）
  ↓
自动预填金额
  ↓
选择补录原因（老人无手机/手机没电/忘记打卡/其他）
  ↓
确认补录 → 默认通过审核
```

---

## 四、数据模型

### 4.1 用户表（users）

| 字段 | 类型 | 说明 |
|------|------|------|
| id | string | 唯一标识 |
| name | string | 姓名（必填） |
| phone | string | 手机号 |
| openid | string | 微信openid |
| assigned_positions | array | 已分配岗位ID列表 |
| role | string | 角色（employee/auditor/chairman/admin） |
| created_at | datetime | 注册时间 |

### 4.2 岗位表（positions）

| 字段 | 类型 | 说明 |
|------|------|------|
| id | string | 唯一标识 |
| name | string | 岗位名称（如：导医、外请导医） |
| type | string | 类型（外请/内部） |
| pay_type | string | 结算方式（按次/按小时） |
| rate | decimal | 单价（如：60/次、25/小时） |
| department_id | string | 所属部门ID |
| need_assessment | boolean | 是否需要考核 |
| auditors | array | 负责审核的审核员ID列表 |

### 4.3 打卡记录表（checkins）

| 字段 | 类型 | 说明 |
|------|------|------|
| id | string | 唯一标识 |
| user_id | string | 用户ID |
| position_id | string | 岗位ID |
| date | date | 打卡日期 |
| time | time | 打卡时间 |
| amount | decimal | 结算金额 |
| hours | decimal | 工作小时数（按小时计费时） |
| status | string | 审核状态（待审核/已通过/已驳回） |
| is_add_record | boolean | 是否为补录记录 |
| add_reason | string | 补录原因 |
| add_by | string | 补录人ID |
| audit_by | string | 审核人ID |
| assessment_status | string | 考核状态（待考核/已通过/扣分/已撤销） |
| assessment_discount | decimal | 考核折扣（1.0/0.8/0.7/0.5/0） |
| created_at | datetime | 创建时间 |

### 4.4 审核员表（auditors）

| 字段 | 类型 | 说明 |
|------|------|------|
| id | string | 唯一标识 |
| user_id | string | 关联用户ID |
| position_ids | array | 负责的岗位ID列表 |

### 4.5 董事长表（chairmen）

| 字段 | 类型 | 说明 |
|------|------|------|
| id | string | 唯一标识 |
| user_id | string | 关联用户ID |
| view_scope | string | 数据查看范围（全公司/指定部门） |

---

## 五、关键逻辑

### 5.1 岗位显示逻辑

```
打卡页 = 我的岗位（已分配） + 临时岗位（常显）

· 新用户（未分配岗位）：只显示临时岗位
· 老用户（已分配岗位）：显示已分配岗位 + 临时岗位
· 临时岗位永远显示：方便临时调岗时使用
```

### 5.2 审核员权限逻辑

```
审核员只能看到/操作自己负责岗位的记录
  ├─ 审核列表：只显示自己负责岗位的待审核记录
  ├─ 分配岗位：只能分配到自己负责的岗位
  ├─ 考核列表：只显示自己负责岗位中需要考核的人员
  └─ 统计：只看到自己所辖岗位的统计数据

扫码人员的打卡记录所有审核员可见，但只能分配到自己管理的岗位
```

### 5.3 补录逻辑

```
补录 = 审核员代填打卡记录
  ├─ 补录即通过：无需再次审核，直接进入已审核列表
  ├─ 姓名自动匹配：输入姓名自动查找已注册用户，填充手机号
  ├─ 同名区分：多个同名用户时提示输入手机号区分
  └─ 金额自动预填：选择岗位后自动填充对应价格
```

### 5.4 考核逻辑

```
考核 = 月底对需考核岗位的人员进行评估
  ├─ 考核选项：考核通过（100%）/ 扣分8折/7折/5折/0折
  ├─ 二次确认：点击考核按钮后弹出确认弹窗，防误点
  ├─ 可修正：已完成考核后可随时修正或撤销考核
  └─ 统计影响：扣分后实发金额 = 原始金额 × 折扣
```

### 5.5 费用统计逻辑

```
费用统计 = 按岗位类型区分（外请/内部），与人员无关

· 外请费用 = 所有"类型=外请"岗位的打卡金额之和
· 内部费用 = 所有"类型=内部"岗位的打卡金额之和
· 外请率 = 外请费用 / (外请费用 + 内部费用)
```

---

## 六、技术方案

### 6.1 前端技术

| 模块 | 技术 | 说明 |
|------|------|------|
| 小程序 | 微信小程序原生开发 | 打卡、审核、考核、董事长看板 |
| H5 | 静态网页 | 扫码注册入口（已合并到小程序） |
| 管理端 | PC Web | 人员管理、岗位管理、审核员管理、统计分析 |

### 6.2 后端技术

| 模块 | 技术 | 说明 |
|------|------|------|
| API接口 | RESTful API | 提供数据读写接口 |
| 数据库 | MySQL/PostgreSQL | 存储用户、岗位、打卡记录等数据 |
| 微信能力 | 微信小程序API | wx.login、getPhoneNumber、小程序码 |

### 6.3 微信能力使用

| 能力 | 用途 | 费用 |
|------|------|------|
| 小程序码 | 用户扫码直接进入小程序 | 免费 |
| wx.login | 获取openid，静默登录 | 免费 |
| getPhoneNumber | 一键获取手机号 | ¥0.03/次，每月1000次免费 |

### 6.4 核心API接口设计

#### 用户相关
- `POST /api/users/register` - 用户注册（姓名、手机号、openid）
- `POST /api/users/login` - 用户登录（微信授权）
- `GET /api/users/me` - 获取当前用户信息
- `GET /api/users` - 获取用户列表（管理端）

#### 岗位相关
- `GET /api/positions` - 获取岗位列表
- `POST /api/positions` - 创建岗位（管理端）
- `PUT /api/positions/:id` - 更新岗位（管理端）
- `GET /api/positions/user` - 获取用户可打卡的岗位（含临时岗位）

#### 打卡相关
- `POST /api/checkins` - 打卡
- `GET /api/checkins/pending` - 获取待审核打卡记录（审核员）
- `POST /api/checkins/:id/audit` - 审核打卡记录
- `POST /api/checkins/add-record` - 补录打卡记录（审核员）

#### 考核相关
- `GET /api/assessments/pending` - 获取待考核列表（审核员）
- `POST /api/assessments/:id/submit` - 提交考核结果
- `POST /api/assessments/:id/correct` - 修正考核结果

#### 统计相关
- `GET /api/stats/overview` - 统计总览（董事长/管理员）
- `GET /api/stats/department` - 部门分析（董事长/管理员）
- `GET /api/stats/auditor` - 审核员所辖岗位统计

---

## 七、页面清单

### 7.1 小程序页面

| 页面 | 角色 | 说明 |
|------|------|------|
| 打卡页 | 所有员工 | 我的岗位卡片 + 临时岗位卡片 + 底部圆形打卡按钮 |
| 审核页 | 审核员 | 待审核列表 + 已审核列表 + 补录打卡按钮 |
| 考核页 | 审核员 | 待考核列表（按岗位分组） + 已完成考核列表（可修正） |
| 我的页 | 所有员工 | 用户信息 + 月度汇总 + 所辖岗位统计（审核员） |
| 董事长看板 | 董事长 | 今日打卡概览 + 审核状态概览 + 部门分布（只读） |
| 董事长统计 | 董事长 | 统计总览 + 部门分析 + 趋势图（只读） |

### 7.2 管理端页面

| 页面 | 说明 |
|------|------|
| 人员管理 | 已注册人员列表 + 岗位分配操作 |
| 岗位管理 | 岗位列表（含类型、结算方式、审核员） + 新增/编辑 |
| 审核员管理 | 审核员列表（按岗位分组） + 新增审核员 + 董事长设置 |
| 考核管理 | 考核结果列表 + 考核进度统计 |
| 统计分析 | 总览（外请/内部费用） + 部门分析 + 月度趋势 + 外请率 |

---

## 八、原型文件说明

### 8.1 temp-worker-miniapp-demo.html

**审核员视角（默认）**：
- 顶部：角色切换按钮（审核员·张经理 / 董事长·姚董）
- 底部Tab：📍打卡 / ✅审核(4) / 🎯考核(2) / 👤我的
- 打卡页：我的岗位（导医、外请导医、绿化工、工程工）+ 临时岗位（常显）
- 审核页：待审核列表（扫码人员需分配、小程序打卡需确认）+ 已审核列表（含补录示例）+ 补录打卡按钮
- 考核页：待考核列表（按岗位分组）+ 已完成考核列表（可修正）+ 二次确认弹窗
- 我的页：个人信息 + 月度汇总 + 所辖岗位月度统计 + 本月审核进度

**董事长视角（切换后）**：
- 底部Tab：📊看板 / 📈统计 / 👤我的
- 看板页：今日打卡概览 + 审核状态概览 + 部门分布（只读）
- 统计页：总览 + 部门分析 + 趋势图（只读）
- 我的页：个人信息 + 权限说明（只读）

### 8.2 temp-worker-demo.html

**人员管理Tab**：
- 注册流程说明（3步）
- 已注册人员列表（姓名、手机号、已分配岗位、操作）
- 岗位分配逻辑说明

**小程序Tab**：
- 用户卡片（已分配岗位）
- 本月汇总
- 我的岗位（已分配）+ 临时岗位（常显）

**管理端Tab**：
- 今日打卡总览
- 待审核记录（可操作）

**部门Tab**：
- 部门列表（名称、类型、人数、负责人）
- 新增部门表单

**岗位Tab**：
- 岗位列表（名称、类型、结算方式、单价、部门、审核员）
- 新增岗位表单

**审核员Tab**：
- 审核员列表（按岗位分组）
- 董事长账号设置
- 新增审核员表单

**考核Tab**：
- 考核进度统计
- 考核结果列表

**统计Tab**：
- 总览（外请/内部费用、人次、工时、外请率）
- 部门分析（各部门支出、外请率）
- 月度趋势图

### 8.3 scan-checkin-flow-demo.html

**扫码注册流程演示**（5个手机并排）：
- 步骤1：微信扫小程序码（扫码框动画）
- 步骤2：微信授权 + 填写姓名 + 获取手机号（一键获取）
- 步骤3：选临时岗位打卡（首次）
- 对比页：已分配岗位用户的打卡页（我的岗位 + 临时岗位）

---

## 九、补充说明

### 9.1 微信获取手机号费用
- 每月免费额度：1000次
- 超出部分：¥0.03/次
- 建议：姓名必填，手机号用微信一键获取（体验好，费用低）

### 9.2 临时工与内部员工区分
- 不区分外请/内部人员，所有人统一扫码注册
- 外请/内部区分只在岗位类型上（岗位表的type字段）
- 统计时按岗位类型汇总费用

### 9.3 临时岗位设计
- 临时岗位永远显示在打卡页
- 新用户只看到临时岗位，老用户看到"我的岗位 + 临时岗位"
- 临时岗位打卡后，审核员在审核时分配真实岗位和金额

### 9.4 补录设计
- 补录由审核员操作，在审核Tab内完成
- 补录记录默认通过审核，无需再次审核
- 姓名自动匹配已注册用户，提高填写效率
- 同名用户提示输入手机号区分

### 9.5 考核防误点
- 点击考核按钮后弹出二次确认弹窗
- 已完成考核后可随时修正或撤销考核
- 月底考核锁定后需联系管理员解锁
