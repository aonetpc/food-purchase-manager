# 工作日志 - 2026-08-23

## 一、近期日志回顾（待跟进项汇总）

### 🟥 未完成的高紧迫项（必须今日处理）

| # | 来源 | 待跟进项 | 说明 |
|---|------|---------|------|
| 1 | 08-22 出错4 教训引申 | **审计午餐/晚餐派生是否同样"叠加计数"** | 早餐是 Math.max，检查 `deriveLunchSessions` / `deriveDinnerSessions` 是否有同样的体检+住宿+自定义相加导致重复计数的 Bug |
| 2 | 08-20（遗留） | **午餐/晚餐下拉分类是否"只显示 4 个不全"** | 之前用户反馈过午餐晚餐类型配置有 7-8 种，但下拉里只看得到 4 种，之前修过一次要确认彻底 |
| 3 | 08-20（遗留） | **8 种业务行展开详情正确性** | 用餐中文名反查/康乐项目名/会务名等是否正确显示 |
| 4 | 08-22 七-5 | **销售用户 fromBackend 策略统一 + api.ts 文件头注释写规范** | 避免以后新增配置再次踩 fromBackend 字段名转换的坑 |

### 🟨 中紧迫项（用户说"等我再想一下"的需求）

| # | 来源 | 待跟进项 | 说明 |
|---|------|---------|------|
| 5 | 08-22 七-1 | 体检分类改名（实验室→实验室检查，特色加项→专科加项）+ 男性 Tab 隐藏妇科专项分类 | 建议1+2 用户看过方案未确认 |
| 6 | 08-22 七-2 | 配套餐公共池改「3角色显式开关勾选」模式（替代关键词黑名单） | 方案 A 用户说"等想一下" |
| 7 | 08-22 七-3 | 临床疾病分组自定义（项目 groups 表+中间表，业务配置 Tab2 自由增删改） | 方案 D1 用户说"等想一下" |
| 8 | 08-20 遗留 | 团队用餐上传菜单（图片/xlsx/混合 4 方案） | 方案出过用户一直未确认 |
| 9 | 08-20 遗留 | 体检展开区三块：摘要/名单 xlsx 导出/套餐查看 xlsx 导出 + 身份证不脱敏 | 前端部分已做，导出 xlsx 接口未完成 |
| 10 | 08-20 遗留 | Sub-Modal 尺寸一致性：套餐查看弹窗与所有业务展开弹窗同宽同高 | |

### 🟩 技术债（有空再做）

| # | 来源 | 待跟进项 | 说明 |
|---|------|---------|------|
| 11 | 08-22 日志 | **Create.tsx / index.tsx 拆分** | 分别 4500+/2900+ 行，建议抽独立子组件 |
| 12 | 08-22 日志 | **CSV 计价方式列**：导入时支持手动覆盖房型默认配置的 pricing_mode |
| 13 | 08-22 出错6 建议引申 | **配套餐项目卡片 hover 气泡 tooltip**：显示项目全名+单位+参考价+临床意义 |

---

## 二、昨日（08-22）核心教训回顾（今日需防再犯）

### 🚨 教训总集（6 条红线，今日必须遵守）

| # | 来源 | 红线规则 | 对应昨日出错编号 |
|---|------|---------|--------------|
| 1 | fromBackend 系统性坑 | **配置类数据（roomTypes/meetingHalls/mealTypes/checkupItems）API 层一律不走 fromBackend，保持 snake_case**；订单/用户类才转 camelCase | 出错1 |
| 2 | 住宿派生餐饮时间线 | **餐饮派生先画横轴日期，再写循环；早餐=入住次日 + 退房当日（for i=1..nights），不是 i=0..nights-1**；前后端同构逻辑必须同时改 | 出错2 |
| 3 | 用户值 vs 配置推算值 | **用户手动输入的 pax/rooms 永远优先，配置推算值（rooms×beds）仅做旧数据兜底** | 出错3 |
| 4 | 派生 vs 叠加 | **同一人多身份（体检+住宿）→ 取 Math.max；不同组加总（体检+外部访客）→ 才用 sum**；午晚餐必须检查同样问题 | 出错4 |
| 5 | UI 改版前消歧 | **改 UI 前先问用户 3 个问题：典型使用设备？信息密度优先还是视觉美观优先？字号最小可接受多少？** 不得靠最佳实践直觉替用户做判断 | 出错5 |
| 6 | 列表样式一致性 | **列表项内只允许"状态差"（选中/未选/禁用），不允许"片段差"（同一项目名字重/颜色不同）**；分段加粗/改色只允许用于搜索高亮或独立标题 | 出错6 |

### 🧱 新增"派生餐饮计数"检查框架（今日审计午餐/晚餐时用）

```
Step 1: 业务拆分 —— 哪些业务是"同一批人"的不同视图？
  体检+住宿+早餐 = 同一团队（取 max）
  午餐/晚餐 = 可能是体检团队 + 外部访客（需要问用户规则）

Step 2: 派生逻辑梳理
  早餐：用户手动 + 住宿派生 + 体检派生 → 取 Math.max
  午餐：？用户手动 + 住宿派生（存在吗？） + 体检派生（默认？）
  晚餐：？

Step 3: 前后端同构检查
  前端 utils.ts deriveXxxSessions() 必须 === 后端 booking-board.js deriveXxxItems()
  任何差异 = Bug
```

---

## 三、今日工作计划

### 阶段 1：预防性审计（上午优先执行，不等用户确认）

#### ✅ 审计 1：午餐/晚餐派生是否重复计数（出错4 兄弟 Bug）
1. `grep -n "deriveLunchSessions\|deriveDinnerSessions" src/pages/BookingBoard/utils.ts`
2. 检查逻辑：体检 + 住宿 + 自定义项 是 `sum()` 还是 `max()`？
3. 检查 booking-board.js 后端同名函数是否同构
4. 如果发现 sum → 出详细修复方案等用户确认（因为午餐/晚餐可能真的有"加桌客人"需求，不能直接改成 max）

#### ✅ 审计 2：用餐（午餐/晚餐）类型下拉显示不全（用户之前反馈"只有 4 个"）
1. 进入 Create.tsx 用餐抽屉，搜索"mealType / lunchType / dinnerType"相关渲染
2. 检查 BizConfigModal 配置页面"用餐类型"表格实际有多少条数据，与 create 页面下拉条目数对比
3. 如果存在问题 → 出修复方案
4. 同时检查：康乐类型/会务类型/用车类型是否有同样问题（系统性 Bug）

#### ✅ 审计 3：salesUsers fromBackend 策略 + 统一写注释规范
1. 读 api.ts 中 salesUsers/meetingHalls/roomTypes/mealTypes 的 4 类配置 API
2. 确认所有 snake_case 字段（如 `sales_person_id / half_price / sort_order / pricing_mode` 等）的渲染入口都用 snake_case 不是 camelCase
3. 在 api.ts 文件顶部写统一的"字段命名策略注释块"

#### ✅ 审计 4：8 种业务行展开详情正确性（08-20 遗留）
1. 点订单 → 午餐/晚餐抽屉 → 检查「用餐类型中文名（不是 code）」「单价」「数量」「小计」显示
2. 会务抽屉 → 会议厅名/时段/半整天价
3. 康乐抽屉 → 项目名/单价
4. 如果有问题 → 单列 Bug 清单

### 阶段 2：用户响应（视情况进行）

等待用户对"需求5~10"（中紧迫项）的确认回复，收到后立即：
- 配套餐分类/角色开关/疾病分组：出详细方案或直接执行
- 用餐菜单上传：出详细方案（之前有 4 方案等用户选）
- 体检展开区导出功能：补后端导出 xlsx 接口

### 阶段 3：技术债清理（空挡进行）

- 优先级：审计 3（写 api.ts 注释规范）→ 配套餐 tooltip（审计 6 引申）→ CSV 计价方式列 → 组件拆分（Create.tsx/index.tsx）

---

## 四、完成事项（实时记录）

_待工作完成后按顺序填写。_

---

## 五、出错的地方（实时记录）

_如有 Bug 修复，按"现象-根因-教训"三段式记录。_

---

## 六、部署记录

| commit | 时间(UTC) | 内容摘要 | 关联文件 |
|--------|-----------|----------|----------|
| — | — | 今日刚启动，等待部署 | — |

---

## 七、明日（08-24）工作准备

_根据今日完成情况和用户反馈，明早填写。_
